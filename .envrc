mkdir -p .direnv/bin

for cmd in go gopls typescript-language-server golangci-lint-langserver; do
  cat > .direnv/bin/$cmd <<SCRIPT
#!/bin/sh
exec docker compose exec -T -w "\$(pwd)" dev $cmd "\$@"
SCRIPT
  chmod +x .direnv/bin/$cmd
done

PATH_add .direnv/bin
